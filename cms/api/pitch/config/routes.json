{
  "routes": [
    {
      "method": "GET",
      "path": "/pitch",
      "handler": "pitch.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pitch",
      "handler": "pitch.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pitch",
      "handler": "pitch.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
